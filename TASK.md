# 完整首轮任务

离线日志中的本地时刻可能因时区回拨而重复，也可能落在跳空区间。请实现时间映射库，只使用调用方提供的转换表，处理点查询、区间映射和有限递推。

实现整数秒时区映射，给定umin<umax的有限UTC半开域[umin,umax)、初始offset及严格递增转换(utc,newOffset)，转换点须在域内部，点上即用新offset；local=utc+offset。不读取系统时区/IANA数据库，不解析真实日历。

UTCToLocal越域报错。LocalToUTC返回域内升序唯一候选；all无候选返回[]，earliest/latest无候选报gap，reject只接受恰好一个候选。多个候选为fold，不假定最多2个。

本地[l,r)映射为所有符合条件的域内UTC区间，升序合并相邻段，不能只转换端点。反向映射UTC区间先裁剪到域内，再输出本地覆盖计数恒定的最大半开段；空区间为空，反向区间报错。

有限递推local_j=start+j*step，step>0、0<=j<count；gap选择skip/reject，fold选择earliest/latest/all/reject。输出(j,utc)按utc再j排序；每个UTC最多对应一个j，每个j可对应多个UTC，不实现无意义的跨j去重。

转换表<=1000项，count<=10000，整数运算精确，用分段区间算法不逐秒扫描。用小UTC域逐秒枚举参考验证多重fold、区间覆盖计数和边界；演示回拨三候选与跳空，不做预约系统。

测试里请覆盖这些情况：跳空、回拨三候选、30秒变化、转换点、域裁剪、空区间、反向区间拒绝、覆盖计数、按UTC重排递推。

使用 Python 3.14.7 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `python demo.py` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `python -m unittest discover -s tests -v` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
