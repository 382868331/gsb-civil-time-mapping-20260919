# 完整首轮任务

请从零实现“显式时区转换表的民用时间映射库”，用于时间算法 / 日历基础库中的本地可复用模块。

1. 从零实现基于显式时区转换表的时间映射库。时间轴使用整数秒，定义在给定有限UTC半开域[umin,umax)；初始offset和严格递增的(utc,newOffset)组成表，local=utc+offset。offset可正负，不读取系统时区或IANA数据库。
2. UTCToLocal唯一；LocalToUTC返回域内全部合法UTC候选，按UTC排序，不假定最多两个候选。无候选为gap，多个为fold；支持reject/earliest/latest/all选择，reject在非唯一时明确错误。
3. 映射本地半开区间[l,r)到所有符合local条件的UTC半开区间，按UTC排序并合并相邻区间；返回的区间并集精确，不能只转换两个端点。反向映射UTC区间到本地覆盖时保留多重覆盖计数。
4. 提供有限递推本地时刻start+j*step（step>0，0<=j<count），gap支持skip或reject，fold支持上述策略；结果按UTC排序并去重，同时保存每个结果对应的全部j来源，不依赖真实日历或自然语言规则。
5. 限制转换表1000项、count10000，用分段区间运算避免逐秒扫描；用小UTC域逐秒枚举作独立参考，覆盖非一小时跳变、多重fold、边界和裁剪。只做映射库，不做预约系统。

验收重点：向前跳空、向后重复、30秒跳变、连续回拨三候选、恰好转换点、跨多段区间、域边界、来源去重。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Python 3.14.7标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；python demo.py须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为python -m unittest discover -s tests -v。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
